<h1>Notes</h1>
<%= link_to 'New Note', new_note_path %>

<ul>
  <% @notes.each do |note| %>
    <li>
      <%= link_to note.content.truncate(30), note_path(note) %>
      (<%= link_to 'Edit', edit_note_path(note) %> |
       <%= link_to 'Delete', note_path(note), method: :delete, data: { confirm: 'Are you sure?' } %>)
    </li>
  <% end %>
</ul>
